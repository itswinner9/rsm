# Resumify (rsm)

Next.js app: ATS-focused resume optimization, Supabase auth/data, Polar billing.

## Local setup

1. `npm install`
2. Copy `.env.example` to `.env.local` and fill in Supabase, OpenRouter, and Polar keys.
3. `npm run dev`

Do not commit `.env.local`.
