"use client";

/**
 * Deprecated no-op. URL cleanup after checkout lives in {@link PaymentSuccessBanner}.
 * This file remains so stale `.next` caches or older imports do not fail the build.
 */
export function DashboardClearSuccessQuery() {
  return null;
}
